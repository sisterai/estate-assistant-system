{{- if .Values.frontend.enabled }}
{{- $annotations := dict -}}
{{- range $k, $v := .Values.global.cloud.service.annotations }}
{{- $_ := set $annotations $k $v -}}
{{- end }}
{{- range $k, $v := .Values.frontend.service.annotations }}
{{- $_ := set $annotations $k $v -}}
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "estatewise.componentName" (dict "root" . "component" "frontend") }}
  namespace: {{ include "estatewise.namespace" . }}
  labels:
    {{- include "estatewise.componentLabels" (dict "root" . "component" "frontend") | nindent 4 }}
  {{- if gt (len $annotations) 0 }}
  annotations:
    {{- toYaml $annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.frontend.service.type }}
  selector:
    {{- include "estatewise.selectorLabels" (dict "root" . "component" "frontend") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: http
{{- end }}
