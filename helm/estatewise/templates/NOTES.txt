Helm release "{{ .Release.Name }}" deployed in namespace "{{ include "estatewise.namespace" . }}".

Services:
{{- if .Values.frontend.enabled }}
- Frontend: {{ include "estatewise.componentName" (dict "root" . "component" "frontend") }}
{{- end }}
{{- if .Values.backend.enabled }}
- Backend: {{ include "estatewise.componentName" (dict "root" . "component" "backend") }}
{{- end }}
{{- if .Values.grpc.enabled }}
- gRPC: {{ include "estatewise.componentName" (dict "root" . "component" "grpc") }}
{{- end }}
{{- if .Values.mcp.enabled }}
- MCP: {{ include "estatewise.componentName" (dict "root" . "component" "mcp") }}
{{- end }}

If you did not create secrets via Helm, ensure {{ .Values.secrets.name }} exists in the namespace.
